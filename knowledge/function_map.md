# Verified MATLAB → Python function map (project-specific; overrides the skill's reference table when they disagree)

Exactly **one** canonical Python target per MATLAB call. Parity labels: `exact` / `near` / `approx` / `reimplemented` /
`unverified` (see seaice-book skill). "Verified in" = chapter whose `reports/chNN_verification.md` holds the evidence.
Never remove a verified row; if a later chapter finds a better mapping, edit the row and note "superseded chNN".

| MATLAB | Python we use | Parity | Verified in | Note |
|---|---|---|---|---|
| `imread('x.jpg')` (JPEG) | `seaice.core.io.load_image(chapter, name)[0]` | exact | ch02, ch03 | imageio/Pillow decode identical to MATLAB on `rgb.JPG` (0 / 9 437 184 samples) and on the three 4290×2856 ch03 files (0 px after `rgb2gray`); resolves case-insensitively (`rgb.jpg` → `rgb.JPG`); falls back to the public-domain substitute registered in `core.public_images` |
| `rgb2gray(I)` | `seaice.core.matlab_compat.rgb2gray_matlab(I)` | exact | ch02 | NTSC 0.298936/0.587043/0.114021 in double, round half away from zero; 0 of 3 145 728 px differ; double input also exact. Not `skimage.color.rgb2gray` (Rec. 709) |
| `imcomplement(I)` | `seaice.core.matlab_compat.imcomplement(I)` | exact | ch02, **corrected and re-verified in ch07** | **canonical** (ch02 review Should-fix 7); `setops.gray_complement` / `bitwise_not` are equation-named forms — do not use them for ports. **Rule: "IM2 has the same class as IM" applies to every branch** — logical → `~I`; **all four** unsigned classes → `intmax(class) − I`; signed → `bitcmp(I)` = `−1 − I`; float → `1 − I` **in the input float class**. ch02 only fixtured uint8/uint16/int8/logical/double, and the float branch was `1.0 - img.astype(np.float64)`: it promoted `single` to `double` (`imcomplement(single(1e-8))` is exactly `1`, `imcomplement(imcomplement(single(0.1)))` = `0.100000024`) and `uint32`/`uint64` fell through to it (`uint32([0 1 2])` → `float64 [1, 0, −1]` instead of `[4294967295, …]`). Fixed in `5186e9d`; **all 11 MATLAB classes now have an L2 reference** (each wrapped in `try/catch` — none refused). It decides `imfill`'s `single` branch (2 of 25 elements at 2.235e-8 before the fix). `imcomplement(double in 0–255)` = `1 − I` |
| `uint8(x)` (cast of doubles in 0–255 units) | `seaice.core.matlab_compat.to_uint8_saturating(x)` | exact | ch02 | **canonical** for the cast: MATLAB round (half away from zero) + saturate; never `astype(np.uint8)` (wraps/truncates) |
| `im2uint8(I)` | `seaice.core.matlab_compat.im2uint8(I)` | exact | ch02 | float [0,1] → `round(255x)` clipped; logical → 0/255; uint8 unchanged. Distinct from `uint8()` above |
| `im2double(I)` | `seaice.core.matlab_compat.im2double(I)` | exact | ch02 | uint8 /255, uint16 /65535, logical 0/1 |
| `double(I)` (no scaling) | `I.astype(np.float64)` | exact | ch02 | `color_image.m` idiom; keep 0–255 units |
| `round(x)` | `seaice.core.matlab_compat.matlab_round(x)` | exact | ch02 | half away from zero (numpy is half-to-even) |
| `imhist(I)`, `[c, x] = imhist(I, n)` | `seaice.core.histogram.imhist(I, nbins=None)` | exact | ch02 | logical → 2 bins at `[0, 1]`; uint8 → 256; uint16/double/n-bin variants identical incl. centres |
| manual `num(k+1)=#(I==k); GP=num/(m*n)` (Eqs. 2.7–2.8) | `seaice.core.histogram.normalized_histogram(I)` | exact | ch02 | `histogram.m` lines 25–28 |
| `bwdist(BW, 'euclidean'/'cityblock'/'chessboard')` | `seaice.core.distance.bwdist(BW, metric)` | exact | ch02, ch05 | distance to nearest **nonzero**; MATLAB returns single → compare `atol=1e-4`; all-zero input → Inf both sides. **Keep the float32 result** when it feeds `watershed`/`imregionalmin`/`imimposemin` (ch05: `ch05_watershed.inverse_distance` = `-bwdist(~bw)` in single; city/chess 0.0, euc/quasi ≤ 1e-4 vs MATLAB, minima and basins identical) |
| `bwdist(BW, 'quasi-euclidean')` | `seaice.core.distance.bwdist(BW, 'quasi-euclidean')` (→ `quasi_euclidean_dt`) | near | ch02 | port float64 chamfer exact to 7e-13 vs analytic; MATLAB single accumulates 1e-4 (rel 8e-7); compare `rtol=1e-5` |
| Book Eq. (2.9) DT (object → nearest background) = `bwdist(~f)` | `seaice.core.distance.distance_transform(f, metric)` | exact | ch02 | use for book-equation code; use `bwdist` for MATLAB scripts |
| `bwlabel(BW, conn)` | `seaice.core.connectivity.label_components(BW, conn=8)` | exact | ch02 | label **numbers** reproduced (column-major first occurrence); `conn` 4 or 8; int32 output |
| `max(L(:))` component count | `seaice.core.connectivity.count_components(BW, conn)` | exact | ch02 | Fig 2.11: 5 / 2 |
| `bwperim(BW)` (not called in ch2; §2.3.5 boundary definition) | `seaice.core.connectivity.region_boundary_mask(BW, conn=8)` | reimplemented | ch02 | text-only; image-border pixels count as boundary. **Superseded ch06** — use `connectivity.bwperim(bw, conn)` (exact vs MATLAB, 9 shapes × conn {4, 8} 0 px) for any port of a real `bwperim`/`bwmorph('perim8')` call; keep this one only for the §2.3.5 teaching definition |
| `conv2(f, w, 'same'/'full'/'valid')`, `conv2(f, w)` (default **`'full'`**) | `seaice.core.filters.conv2(f, w, mode)` (pass `'full'` explicitly for the no-mode call) | exact | ch02, ch04 | true convolution, zero padding; even-kernel centring reproduced (≤ 3.6e-15); `derivative.m` line 15 `conv2(double(BW), double(msk))` grows 2856×4290 → 2860×4294, identical (max 6) |
| `imfilter(f, w)`, `imfilter(f, w, 'replicate'/'symmetric'/'circular'/X, 'conv'/'corr', 'same'/'full')` | `seaice.core.filters.imfilter(f, w, *options)` | exact | ch02 | correlation by default; MATLAB positional string options accepted in any order (or keywords `mode=`, `padding=`, `output=`); 3-channel input ok; ≤ 5.3e-15 |
| `boundaries(BW, conn, dir)` (DIPUM, `ch2/chain code/`, copies in `ch5/`) | `seaice.core.chaincode.boundaries(BW, conn=8, direction='cw')` | exact | ch02 | list index = `bwlabel` label − 1; arrays `(Q, 2)` 0-based (row, col), closed; exterior only; `'ccw'` reverses; `'cww'` typo in `chain_diff.m` = `'cw'` |
| `fchcode(b, conn, dir)` (DIPUM) | `seaice.core.chaincode.fchcode(b, conn=8, direction='same')` → `ChainCode(fcc, diff, mm, diffmm, x0y0, conn, x0y0_matlab)` | exact | ch02 | `x0y0` 0-based, `x0y0_matlab` 1-based; `'reverse'` = `code_reverse`; conn validated first |
| `fchcode` local `minmag` on periodic / length-1 codes | `seaice.core.chaincode.min_magnitude(code)` | reimplemented | ch02 | MATLAB **errors** (`Output argument "z" not assigned`); port returns the lexicographic minimum rotation (= book Fig 2.21 last line). ch5 `try/catch` caveat |
| `fchcode` local `codediff` (Eq. 2.31) | `seaice.core.chaincode.first_difference(code, conn)` | exact | ch02 | `(C(i+1) − C(i)) mod conn`, circular |
| `bound2im(b)`, `bound2im(b, M, N)`, `bound2im(b, M, N, x0, y0)` (DIPUM) | `seaice.core.chaincode.bound2im(b, M=None, N=None, x0=None, y0=None)` | exact | ch02 | `x0, y0` 0-based (MATLAB `2,3` ↔ Python `1,2`); `b` may be `(Q,2)` or `(2,Q)` |
| `cellfun('length', b)` on boundary cells | `[len(x) for x in b]` | exact | ch02 | `chain_diff.m` |
| `interp2(X, Y, V, Xq, Yq, 'nearest'/'linear'/'cubic')` (unit grid) | `seaice.core.interp.interp2(V, u=Yq−1, v=Xq−1, method)` | exact | ch02 | 0-based (row, col); nearest tie = half away from zero; cubic = Keys `a=−0.5` + quadratic edge extrapolation (`pad='quadratic'`, default) exact incl. borders (6e-13); outside → NaN |
| `imresize(I, s, 'nearest'/'bilinear', 'Antialiasing', false)` (enlarge) | `seaice.core.interp.resize(I, s, method)` | exact | ch02 | 0.0 on full arrays for ×2, ×4 and ×0.5 (antialias off); output size `ceil(s·M)`, pixel-centre convention |
| `imresize(I, s, 'bicubic')`, any shrink with antialiasing | `seaice.core.interp.resize(I, s, 'bicubic')` | approx | ch02 | border differs (≤ 8.2 levels, 5.6 % px within 1.25 source px of the edge; interior 0.0); no antialiasing. Implement `imresize_matlab` if a chapter needs it numerically (ch9?) |
| `ind2rgb(idx, cmap)` | `seaice.core.color.indexed_to_rgb(idx, cmap, one_based=False)` | exact | ch02 | integer classes 0-based, double 1-based (`one_based=True`), out-of-range clipped like MATLAB; non-integer doubles truncated where MATLAB raises |
| `imcomplement(I)` used as CMY (Eq. 2.3) | `seaice.core.color.rgb2cmy(I)` | exact | ch02 | thin wrapper; same result as `imcomplement` |
| HSI block of `color_image.m` (Eqs. 2.6a–c) | `seaice.core.color.rgb2hsi(I, use_atan2=False, matlab_bug=False, scale=None)` | reimplemented (exact vs corrected MATLAB; `matlab_bug=True, scale=255` exact vs script) | ch02 | default = book (`2*Ib`); script bug `2*Ig` reproduced on request; `atan` undefined at `V1 = 0` (1 px of `rgb.JPG` on [0,1] input) |
| CMYK Eqs. (2.4)–(2.5) (no MATLAB builtin; `rgb2cmyk` is ICC-based) | `seaice.core.color.rgb2cmyk(I, u=1.0, b=1.0)` | reimplemented | ch02 | 0.0 vs hand-coded MATLAB Eq. 2.4 |
| `I(:,:,1)`, `I(:,:,2)`, `I(:,:,3)` | `seaice.core.color.split_rgb(I)` | exact | ch02 | dtype preserved |
| `zeros(201,201); img(101,101)=1` (Fig 2.14a) | `seaice.core.synth.point_image(201)` | exact | ch02 | 0-based centre (100,100) |
| `imshow(I)` / `imshow(I, [])` / `mat2gray` | `seaice.core.plotting.imshow_matlab(ax, I, autoscale=False/True)`, `imshow_scale`, `save_image` | display (exact scaling; PNG ±1 level from MATLAB float32 `mat2gray`) | ch02 | numerics never pass through here |
| `saveas(gcf, 'x.png')`, `figure`, `subplot` | `seaice.core.plotting.finish_figure(fig, path, show)` + matplotlib | — | ch02 | scripts write to `outputs/chNN/` |
| `padarray(A, [1 1], 0)` (inside `boundaries.m`) | `np.pad(A, 1, mode='constant')` | exact | ch02 | internal to `chaincode.boundaries` |
| `sub2ind` / `ind2sub` (column-major) | `np.ravel_multi_index(..., order='F')` / `np.unravel_index(..., order='F')` | exact | ch02 | used inside `bound2im`, `boundaries` |
| `[level, em] = graythresh(I)` | `seaice.core.threshold.graythresh(I) -> (level, em)` | exact | ch03 | `im2uint8` → `imhist(256)` → `otsuthresh`; **tie averaging** (`mean(find(σB² == max))`) → `255·level` may be a half-integer (104.5); `em` = book η(t*) (Eq. 3.22); constant image → (0, 0). RGB is **not** converted (MATLAB histograms all planes) — pass `rgb2gray_matlab(rgb)` unless the script itself passes the RGB (ch05: `graythresh(q.jpg RGB)` = 130/255 vs `graythresh(gray)` = 128/255, different masks; `ch05_watershed.otsu_mask` reproduces both). Not `skimage.filters.threshold_otsu` (no tie averaging, image units). Bit-identical on 3 × 12-Mpx JPEGs + 10 compat arrays |
| `[t, em] = otsuthresh(counts)` | `seaice.core.threshold.otsuthresh(counts)` | exact | ch03 | R2025a line-by-line; `t = (idx − 1)/(nbins − 1)`, `em = maxval/(Σ p k² − mu_t²)`; works on any bin count |
| `im2bw(I, level)`, `im2bw(I)` (0.5) | `seaice.core.threshold.im2bw(I, level=0.5)` | exact | ch03 | strict `>` compared in **double** (`uint8([104 105 106]) > 104.5 = [0 1 1]` confirmed in R2025a — never round the level); uint8 `255·level`, uint16 `65535·level`, int16 offset, float `> level`; RGB → `rgb2gray_matlab`; logical passthrough + warning; `level ∉ [0,1]` raises. All 256 levels identical; 0 px on the 3 book images |
| `[thresh, metric] = multithresh(I, N)`, `N ≤ 2` | `seaice.core.threshold.multithresh(I, N)` | exact | ch03 | `getpdf`: `single(A − minA)/single(maxA − minA)` **and** `grayto8`'s `x·255` in single (both emulated); N = 1 vector / N = 2 full 256×256 σB² matrix with NaN masking, ties `mean([maxR maxC]) − 1`; `map2OriginalScale` → output in the **input class** (uint8/uint16/double, MATLAB rounding); `metric` = Eq. 3.28 ≤ 1e-12. Degenerate (`numel(unique) ≤ N`) paths `[0 1]`, `[1 77]`, `[0 1 255]`, `[1 100 255]` identical |
| `multithresh(I, 3)` | `seaice.core.threshold.multithresh(I, 3)` | reimplemented | ch03 | MATLAB uses `fminsearch` (local, `TolX = 1`; may return its initial guess with `metric = −Inf`); port maximises Eq. 3.27 exhaustively with `t1 = 0` allowed — identical on test.jpg/2.jpg/ramp, better σB² on 1.jpg (49,86,160 vs 50,101,165). `N > 3` raises (`skimage.filters.threshold_multiotsu` is only an approximation) |
| `multithresh(int16 image, N)` | `seaice.core.threshold.multithresh` | approx | ch03 | `multithresh.m` line 274 `single(A − minA)` saturates in int16 arithmetic (not emulated; strict xfail); never a book case |
| `imquantize(A, levels)`, `imquantize(A, levels, values)` | `seaice.core.threshold.imquantize(A, levels, values=None)` | exact | ch03 | `index = 1 + Σ_i (A > level_i)` (Eq. 3.23, classes `1..N+1`), comparison in double; strictly increasing `levels` enforced; `seg` 0 px on the 3 images |
| `im2uint8(uint16 I)`, `im2uint8(int16 I)` | `seaice.core.threshold._im2uint8_any(I)` | exact | ch03 | `round(v/257)` (= `(v + 128)/257` integer), int16 `+32768` first; all 65 536 uint16 values identical. `matlab_compat.im2uint8` (ch02 row) still takes the ×255 float path for uint16 — merge pending (ch03 open item 3) |
| `local_Otsu.m` / `ch9/block_threshold.m` block loop (`graythresh` per `I(t1:t2, t3:t4)` block, `th = t*255`, `num`, `IC_local`, `IC`) | `seaice.core.threshold.block_otsu(gray, n_r, n_c) -> BlockOtsu` | exact | ch03 | block index row-major `i·n_c + j` = script `(i−1)·n_c + j`; raises `ValueError` where MATLAB's non-integer indices error; `thresh`/`num`/`IC_local`/`IC`/tiles identical on the 2856×4290 substitute |
| `num2str(x)` (scalar double, default format) | `seaice.ch03_ice_pixel_detection._num2str(x)` | exact | ch03 | integers `%d`; else `%.{n}g` with `n = max(floor(log10|x|) + 5, 5)` → `73.8472`, `86.496`, `3.1416`, `0.12346`, `1234.5679`; NaN/Inf handled; 10 samples + 12 titles byte-identical. Promote to `matlab_compat` when a second chapter needs it |
| `separability.m` (η with 1-based bin index as intensity, `bw = I > k`) | `seaice.ch03_ice_pixel_detection.separability_script(gray, k)` | exact | ch03 | script `eta` = book η(k − 1) (off-by-one, reproduced literally, `eta_book` also returned); `p`, `mg` (= mG + 1), `sigma2_g`, `sigma2_b`, `eta`, `IC`, `bw` ≤ 1e-12 / 0 px for k = 107, 108 |
| Eqs. (3.3)–(3.22) Otsu curves for every `t` (hand-coded MATLAB loops) | `seaice.core.threshold.otsu_criterion(counts) -> OtsuCurves` | exact | ch03 | P0/P1/m/m0/m1/σ0²/σ1²/σW²/σB²/η ≤ 5.5e-11 where both classes non-empty; NaN pattern identical for empty C1; `t_star`/`eta_star` = `graythresh`; identities 3.10/3.11/3.18 to 1e-9 |
| Book separability `η(t)` (Eqs. 3.20/3.22, C0 = levels 0..t) | `seaice.core.threshold.separability(gray, t)` | exact | ch03 | = `graythresh` `em` at t*; η(125) identical to hand-coded MATLAB; 0 for constant, 1 for two-valued {0,255} |
| `ch3/kmeans.m` (authors' histogram k-means; **shadows** Statistics Toolbox `kmeans`) | `seaice.core.clustering.kmeans_gray(gray, k, shift_bug=True)` | exact | ch03 | equal-division init `(1:k)·m/(k+1)` on `ima − min + 1`, `argmin |v − mu|` first-index ties, histogram-weighted update, exact `mu == oldmu` stop; `shift_bug=True` reproduces the units bug of lines 64–70 (**required for every book k-means number**); `mu` 0.0, `n_iter` identical, mask 0 px on 6 runs. Empty cluster → `ValueError` (MATLAB loops forever) |
| `kmeans.m` with Eqs. 3.36–3.37 in consistent units (book text) | `seaice.core.clustering.kmeans_gray(gray, k)` (default `shift_bug=False`) | reimplemented | ch03 | same centroids; mask differs from the script by 0.29–24 % of pixels (levels in `(mid, mid + min − 1]`); k=2 IC 15.36/32.05/95.36 % vs book 15.65/32.49/96.50 % |
| Statistics Toolbox `kmeans(X, k, 'EmptyAction', 'singleton', …)` (ch6/ch7/ch9) | `seaice.core.clustering.kmeans_lloyd(X, k, init='kmeans++', seed=0)` | approx | ch06 (**decided**; see the ch06 row below for the measurements) | **not** `sklearn.cluster.KMeans` and **not** ch3's shadowing `kmeans_gray` — ch6/ch7/ch9 ship no `kmeans.m`, so the call resolves to the toolbox (R2025a defaults `sqeuclidean`/`plus`/1 replicate/100 iterations). Compare sorted centres and pixel agreement, never labels |
| `pdist2`-style distances / Eqs. (3.29)–(3.34) | `seaice.core.clustering.pairwise_distance(X, Y, metric, cov)` | exact (closed forms, L1 only) | ch03 | `'chebyshev'` = max-norm, `'cosine'` = 1 − aᵀb/(|a||b|) (book prints the similarity), `'mahalanobis'` with `cov` or pooled covariance; no `pdist2` reference yet |
| Lloyd k-means Eqs. (3.35)–(3.37) (text only, Figs 3.6/3.8) | `seaice.core.clustering.kmeans_lloyd(X, k, init, seed, max_iter, tol=0)`, `objective_J` | reimplemented | ch03 | ties → lowest index; empty cluster keeps its centroid; `'equal'` divides `[min, max]` whereas `kmeans.m` divides `[min − 1, max + 1]` (pass the vector explicitly); reproduces `kmeans_gray` on 1-D data with that start |
| `ic = length(find(bw == 1))/(r*c)`, `coverage(i)`, `n(i)/(M N)` | `seaice.core.threshold.ice_concentration(mask)`, `class_coverage(labels, k)` | exact | ch03 | fractions in [0, 1] (scripts print %) |
| `average_intensity(i) = sum(s(:))/numel(p)` with the stale `s`/`ss` buffer (`Otsu.m` 39–42, `kmeans.m` 76–79) | `seaice.ch03_ice_pixel_detection.stale_mean_intensity(gray, labels, k)` (script) / `seaice.core.threshold.class_mean_intensity` (correct means) | exact | ch03 | buffer filled column-major by `find`, never shrinks → MATLAB prints 4759.10 on 1.jpg; `sum(uint8 vector)` returns **double** (no saturation), so the emulation sums in float; ≤ 1e-9 on all 9 runs |
| `sum(x)` on a uint8/uint16 vector | `np.sum(x, dtype=np.float64)` | exact | ch03 | MATLAB integer `sum` accumulates in double (`sum(uint8([200 200])) = 400`, class double) |
| `colormap('default')` after `imshow(mask1)` (double) | display only — save gray (`plotting.save_image`) | — | ch03 | MATLAB renders the `i/k` mask in parula; the book prints gray |
| Fig. 3.4(a) "factitious uneven illumination" (text only) | `seaice.core.synth.uneven_illumination(img, gain, axis, bias, offset, kind)` | reimplemented (synthetic) | ch03 | ×(1 + gain) + bias → ×(1 − gain) − bias ramp, `uint8()`-style saturation; RGB or gray |
| `im = double(im)/256` (`derivative.m` line 5) | `im.astype(np.float64) / 256` (literal, inside `seaice.ch04_ice_edge_detection.sobel_edges_script`) | exact | ch04 | **not** `im2double` (/255): the book's `T = 0.05` and `edge`'s automatic threshold refer to the /256 scaling; 0 diff on 12.25 Mpx. `core.edges.edge` refuses integer/bool input so the scaling is always explicit |
| `[BW, thresh, gv, gh] = edge(I, 'sobel'\|'prewitt', T, 'both'\|'horizontal'\|'vertical', 'thinning'\|'nothinning')` | `seaice.core.edges.edge(a, method, thresh=None, direction='both', thinning=True) -> EdgeResult(bw, thresh, gv, gh)` | exact | ch04 | R2025a `edge.m` + the compiled `computeEdges` thinning reverse-engineered: `fspecial/8` (`/6`) kernels, `imfilter(...,'replicate')`, `b = kx·bx² + ky·by² > T²` (auto `4·mean(b)`, `thresh = sqrt`), thinning on a **zero-padded** `b` with `\|bx\| >= kx·\|by\| − 100eps` / `\|by\| >= ky·\|bx\| − 100eps`, `<=` left/up, `>` right/down; Prewitt filters with the integer kernel then divides. 196 maps 0 px (incl. constructed ties, all directions, with/without thinning, auto/given T), `derivative.m` 120 224 px 0 diff, `gv`/`gh` ≤ 1.1e-16, thresholds rel ≤ 1e-12. Float input only (float32 promoted → single-precision MATLAB matched only to 1e-7). Not `skimage.filters.sobel` (magnitude only) |
| `edge(I, 'roberts', T, dir, 'thinning'\|'nothinning')` | `seaice.core.edges.edge(a, 'roberts', …)` (→ `gradient_roberts`) | exact | ch04 | masks `[1 0; 0 −1]/2`, `[0 1; −1 0]/2`, auto scale 6, thinning offsets `[−1 1 1 −1]` (diagonal neighbours); 56 maps 0 px incl. `'horizontal'`/`'vertical'` (`review_followup.mat`); not in the book |
| `[BW, thresh] = edge(I, 'log', T, sigma)`, `edge(I, 'zerocross', T, H)` | `seaice.core.edges.edge(a, 'log', thresh, sigma=2.0)` / `edge(a, 'zerocross', thresh, H=H)` (→ `log_zero_crossings`) | exact on images; **near** on piecewise-constant synthetic input | ch04 | `fspecial('log', 2·ceil(3σ)+1, σ)`, `op −= mean(op)`, replicate `imfilter`; edge = negative side of a sign change with the right/lower (or left/upper) neighbour whose **jump** `\|b(p) − b(q)\| > T` (`2T` at exact zeros), interior only; auto `T = 0.75·mean\|b\|`. `test.jpg` 117 089 px / crop 1885 px 0 diff, 14 synthetic maps 0 px; on a step/constant image ~1e-17 rounding-noise signs differ (7 / 14 px) — cannot be made bit-exact |
| `edge(I, 'canny')` | **not ported** — `core.edges.edge(a, 'canny')` raises `NotImplementedError`; `skimage.feature.canny(a, sigma=np.sqrt(2))` would be `approx` | unverified | — (not in the book) | port R2025a `edge.m`'s canny branch if ch6's GVF code needs it |
| `fspecial('sobel')`, `'prewitt'`, `'laplacian'[, α]`, `'gaussian'[, hsize, σ]`, `'log'[, hsize, σ]`, `'average'[, hsize]`, `'disk'[, r]`, `'unsharp'[, α]` | `seaice.core.filters.fspecial(kind, p2=None, p3=None, *, hsize, sigma, alpha, radius)` | exact | ch04 | R2025a `fspecial.m` formulas and defaults (gaussian 3×3/0.5, log 5×5/0.5, laplacian α 0.2, average 3×3, disk 5; only-σ → `2·ceil(2σ)+1`); `'log'` = unit-sum Gaussian × `(x²+y²−2σ²)/σ⁴`, mean-subtracted (≠ Eq. 4.15 sampled); `'gaussian'` zeroes entries `< eps·max`; 26 kernels ≤ 6.9e-18; Fig. 4.5(a)/(b) 25 + 25 printed values to 4 dp. Unambiguous prefixes accepted |
| `strel('disk', r)`, `strel('disk', r, n)`, `strel('dis', r)` (prefix) | `seaice.core.morphology.strel('disk', r[, n])` (bool nhood = `getnhood(strel(...))`) | exact | ch04 | Adams' periodic-line decomposition (default n = 4 → **octagon**, r < 3 or n = 0 → Euclidean): r = 5 → 9×9/69 px (printed Fig. 4.7(c)), 7 → 13×13/157, 15 → 29×29/697, 16 → 31×31/817; 80 disk nhoods (r = 1…20 × n ∈ {0,4,6,8}) identical; `'di'` ambiguous → `ValueError` (MATLAB errors too); positional `N` vs keyword `n` conflict raises. **Never** `skimage.morphology.disk(r)` / `cv2.getStructuringElement(MORPH_ELLIPSE)` for parity (Euclidean, different shape) |
| `strel('diamond', M)`, `'square'`, `'rectangle', [m n]`, `'line', len, deg`, `'octagon', M`, `'periodicline', p, v`, `'pair', off`, `'arbitrary', nhood` / `strel(nhood)` | `seaice.core.morphology.strel(shape, *params)`; helpers `periodic_line(p, v)`, `line_strel(len, deg)` | exact | ch04 | 36 non-disk nhoods identical to `getnhood`; Fig. 4.7(d) `strel('diamond', 5)` 11×11/61 px; `'line'` uses `iptui.intline` (round half up); 3-D shapes raise `NotImplementedError` |
| `decompose(strel('disk', r, n))`, `getnhood(se)` of a decomposed strel, `floor((size(nhood)+1)/2)` origin, `iptui.intline` | `seaice.core.morphology.disk_decomposition(r, n=4) -> list`, `minkowski_sum(seq)`, `se_origin(se)`, `intline(x1, x2, y1, y2)` | exact | ch04 | 11 decomposition sequences element-by-element in MATLAB's order (r = 7: (5,1), (3,3), (1,5), (3,3), (1,5), (5,1)); `imerode`/`imdilate` with the list equal the flat-nhood result (Minkowski-sum identity) and are faster on 12-Mpx images |
| `imerode(I, SE)` (logical, uint8, uint16, int16, single, double, int32, uint32) | `seaice.core.morphology.imerode(I, se)` (`se` = nhood or decomposed list) | exact | ch04 | Eq. 4.20 min over the SE; out-of-image = 1 / intmax / +Inf; dtype preserved; OpenCV `BORDER_CONSTANT` for bool/uint8/uint16/int16/float32/float64, numpy shift-and-reduce for int32/uint32 (40 int32/uint32 arrays with intmax/intmin plants 0 px); `morphology.m` `J`/`X` 0 px on 12.25 Mpx; 108 compat arrays 0 diff incl. asymmetric/even SEs; float32 promoted to float64 |
| `imdilate(I, SE)` | `seaice.core.morphology.imdilate(I, se)` | exact | ch04 | Eq. 4.21 max over the **reflected** SE (Eq. 4.18/4.19; anchor mapped to `(M−1−r0, N−1−c0)` — OpenCV's `dilate` is non-reflected); out-of-image = 0 / intmin / −Inf; `K`/`Y` 0 px; the asymmetric-SE compat case decides the convention |
| `imerode` / `imdilate` / `imopen` / `imclose` on **int64 / uint64** images | same functions (numpy pad + shift-and-reduce in the native dtype) | reimplemented (L1 only) | ch04 | MATLAB R2025a **rejects** int64/uint64 (`imerode`: "Expected input … to be one of: single, double, int8, int16, int32, uint8, uint16, uint32, logical") → no reference can exist; verified against literal Eq. 4.20/4.21 loops and cross-backend equality (int16 = int32 = int64, 0 px). `scipy.ndimage.minimum_filter/maximum_filter` route `cval` and accumulation through float64 (intmax → intmin, values > 2⁵³ corrupted) — never use them here. Cast `label_components` int64 output to int32 before comparing with MATLAB |
| `imopen(I, SE)` | `seaice.core.morphology.imopen(I, se)` | exact | ch04 | Eq. 4.23 `imdilate(imerode(A, se), se)` on the **unpadded** image (R2025a `imopen.m` line 44); 54 image × SE cases + 1-D profile 0 diff |
| `imclose(I, SE)` | `seaice.core.morphology.imclose(I, se)` | exact | ch04 | **not** the bare composition: `imclose.m` pre-pads `padarray(A, ceil(size(getnhood(se))/2), 'both')` with **0**, dilates, erodes, crops; `morphop_fast.m` routes SEs with nnz < 600 ∧ every side ≤ 15 ∧ not an all-ones rectangle to the Halide kernel padded with the **class minimum** (−Inf / intmin / false; `images.UseHalide` = 1 by default). Identical for uint8/logical/non-negative double, differs for negative values (80 px discriminations proven on `imclose_pad.mat`). 54 + 18 cases 0 px; 43 of 54 failed before the rule was reproduced |
| `imreconstruct(marker, mask)`, `imreconstruct(marker, mask, conn)` | `seaice.core.morphology.imreconstruct(marker, mask, conn=8)` (→ `skimage.morphology.reconstruction(..., 'dilation', footprint)`) | exact | ch04 | Eqs. 4.24–4.27 / 4.31–4.34 fixed point (same result, different algorithm; cross-checked by `reconstruct_iterative`); `marker <= mask` enforced (`ValueError`, MATLAB errors); `conn` 4 / 8 / any nhood (MATLAB needs 3×3 — the port is a superset); dtype preserved; 7 references (binary 8/4, uint8 8/4/cross, double, 1-D) 0 diff. ch05: `±Inf` in marker/mask handled identically (basis of `imimposemin`); **a `{0,1}` marker reconstructs to `{0,1}`**, never to the mask's label value (`main.m` `im = imreconstruct(g, bwlabel(neighbor))` is a 0/1 double) |
| `imcomplement(imreconstruct(imcomplement(marker), imcomplement(mask)))` — reconstruction by erosion, Eqs. 4.28–4.30 / 4.35–4.38 (no builtin) | `seaice.core.morphology.reconstruct_by_erosion(marker, mask, conn=8)` | reimplemented | ch04 | `marker >= mask` enforced; equals the complement identity on the 1-D profile (0 diff) and the hand truth `max(f, floor)` |
| Geodesic dilation/erosion of size n (Eqs. 4.25–4.26, 4.32–4.33), literal `X_k` loops (Eqs. 4.27, 4.30, 4.34, 4.38) | `seaice.core.morphology.geodesic_dilation(F, G, se=None, n=1)`, `geodesic_erosion(...)`, `reconstruct_iterative(F, G, se=None, method='dilation') -> (X, k)` | reimplemented | ch04 | teaching forms; iteration = `imreconstruct` = MATLAB (two-blob fixture k = 11; 1-D k = 46 / 37) |
| `BW1 = I − J` (logical − logical → **double**), `internal = im − X` (uint8 − uint8, saturating), Eqs. 4.39–4.42 | `seaice.core.morphology.morphological_gradient(I, se, kind='basic'\|'internal'\|'external', *, eroded=None, dilated=None)` (`_matlab_minus`) | exact | ch04 | bool in → float64 0/1 out (MATLAB class confirmed); integer in → same class, computed in int64 and clipped; float → float64; precomputed `J`/`K`/`X`/`Y` accepted (shape/dtype checked); `morphology.m` 6 gradient arrays 0 px / 0 levels, Eq. 4.42 identity (698 679 + 811 772 = 1 510 451) |
| `bwareaopen(BW, P)`, `bwareaopen(BW, P, conn)` | `seaice.core.connectivity.bwareaopen(bw, P, conn=8)` | exact | ch04 | keeps components with **≥ P** pixels; built on `label_components` (= `bwlabel`); `BW_ao` 95 564 px 0 diff on `test.jpg`, P = 20 / 4-conn / P = 5 compat cases 0 px. Supersedes the skill's `skimage.morphology.remove_small_objects` mapping (its threshold parameter changed in 0.26) |
| `medfilt2(I, [m n])`, `medfilt2(I, [m n], 'symmetric')` | `scipy.ndimage.median_filter(I, size=(m, n), mode='constant', cval=0)` / `mode='reflect'` | exact | ch04 | MATLAB zero-pads by default (scipy default `reflect` is the `'symmetric'` option); `im_med` on `test.jpg` and the compat arrays 0 diff |
| `derivative.m` (whole script) | `seaice.ch04_ice_edge_detection.sobel_edges_script(gray, T=0.05, method, sigma, median=False, min_area=None, smooth=False)`; `scripts/ch04_derivative.py` | exact | ch04 | `BW` 0 px, `gv`/`gh` ≤ 1e-16, `msk`/`r`/`c` identical; commented lines 6/8/15 behind flags (`BW_med` 115 436, `BW_ao` 95 564, `BWc` 2860×4294 identical) |
| `morphology.m` (whole script) | `seaice.ch04_ice_edge_detection.morphological_edges(gray, se=None, radius=7) -> dict` (12 arrays); `scripts/ch04_morphology.py` | exact | ch04 | `level` bit-identical (0.44313725490196076), `I`/`J`/`K` and the 6 gradients 0 px / 0 levels; r = 15 crop configuration (Figs. 4.9/4.10/4.15/4.16) 0 px |
| `im(1600:2151, 1979:2552, :)` — the Fig. 4.3(a) crop (not in the code; located by template matching) | `seaice.ch04_ice_edge_detection.FIG_4_3A_CROP = (slice(1599, 2151), slice(1978, 2552))`, `fig_4_3a(im)` | exact (data mapping) | ch04 | NCC 0.99886 vs the PDF bitmap (stored inverted) at offset (0, 0), best within ±8 px and scale 0.97–1.03; crop-then-process semantics |
| Hand-coded Eqs. 4.1–4.8 (`imfilter(f, k, 'replicate')` with the Fig. 4.2 kernels as printed, `sqrt`, `atan2`, `> T`) | `seaice.ch04_ice_edge_detection.gradient_operator(f, kind='sobel'\|'prewitt'\|'forward', padding)`, `gradient_magnitude(Gx, Gy, kind='l2'\|'squared'\|'l1')`, `gradient_direction(Gx, Gy, convention='book'\|'math')`, `threshold_gradient(mag, T)` | reimplemented | ch04 | ≤ 1e-15 vs hand-coded MATLAB; book x = row; `Gx_book = −8·gh`, `Gy_book = −8·gv` (−6 Prewitt) to 1e-14; Eq. 4.3 printed `arctan(Gx/Gy)` → `atan2(Gx, Gy)` (`'book'`) |
| Hand-coded Eqs. 4.9–4.13 (Fig. 4.4 kernels), Eq. 4.10 forward second difference, p. 64 3×3 zero-crossing rule | `seaice.ch04_ice_edge_detection.laplacian(f, neighbors=4\|8, padding)`, `second_difference_forward(f, axis)`, `LAPLACIAN_KERNELS`, `laplacian_zero_crossings(lap, T)` | reimplemented | ch04 | ≤ 2.7e-15 vs MATLAB `imfilter`; `fspecial('laplacian', 0)` = Fig. 4.4(a); the p. 64 rule (`imdilate(max(L,0))`/`−imdilate(−min(L,0))` difference > T) is a different detector from `edge('log')` |
| Eq. 4.14 Gaussian, Eq. 4.15 LoG sampled analytically, "Gaussian ∗ Laplacian" | `seaice.ch04_ice_edge_detection.gaussian_kernel(size, sigma, normalize=True)`, `log_kernel(size, sigma, mode='matlab'\|'analytic'\|'gauss_conv_laplacian')` | reimplemented (analytic) / exact (`mode='matlab'` = `fspecial('log')`) | ch04 | analytic samplings 0.0 vs MATLAB; analytic centre −1/π vs Fig. 4.5(b) −0.3182 (mean-subtracted `fspecial`) |
| Fig. 4.8(a)/(b)/(d)/(f) matrices (DIPUM example), 1-D profiles of Figs. 4.11–4.14, marker/mask fixture | `seaice.core.synth.FIG_4_8_IMAGE`, `FIG_4_8_SE`, `FIG_4_8_ERODED`, `FIG_4_8_DILATED`, `two_floes_profile(n=240)`, `two_blobs_with_marker(shape)` | exact (printed truth) / reimplemented (fixtures) | ch04 | MATLAB `imerode`/`imdilate` of (a) by (b) reproduce (d)/(f); a `1×N` image with a `1×k` line SE is exactly the book's 1-D case |
| `L = watershed(A)`, `watershed(A, 4)`, `watershed(A, 8)`, `watershed(A, [0 1 0;1 1 1;0 1 0])`, `watershed(A, ones(3))` | `seaice.core.watershed.watershed(A, conn=8) -> int32` (re-exported as `seaice.core.watershed_transform`) | exact | ch05 | line-by-line port of R2025a `eml/watershed.m` (readable codegen twin of the compiled `images.internal.builtins.watershed_meyer`): seeds `bwlabel(imregionalmin(A, conn), conn)` (= `label_components` numbering), FIFO priority queue = `heapq` on `(priority, insertion order)` (`FifoPriorityQueue.m` sift rules are exactly lexicographic), **initial push scan in column-major order** (order-sensitive: the transposed image differs in 31 of 80 cases and MATLAB matches column-major on all), neighbour scan order inside the loop provably irrelevant (12 permutations × 80 cases = 0 diff), a popped pixel seeing two labels stays 0 **and pushes nothing**, each pixel pushed once with priority `max(A(nb), p)`. **Label values compared**, not just partitions: 74 fixture cases × {8,4} (plateaus, corridors, asymmetric adjacency ± transpose, random uint8 200×300 with 6772/12 036 basins → MATLAB class **uint16**, ±Inf, border minima, 1×N, logical) + every script image (`q.jpg` gray 160, Sobel 350, close-opened 12, city 4 / chess 2 / euc 6 / quasi 6 basins, imposed maps, 552×574 crop 11) 0 px. MATLAB **rejects int16/int32** input (uint8/uint16/single/double/logical only; int16 fixtures referenced on `double(X)`); output class uint8 ≤ 255 regions / uint16 above → cast to int32; 3×3 matrix `conn` accepted only as the cross (4) or `ones(3)` (8) (`conn_to_scalar`); N-D / conn 6/18/26 not ported; NaN rejected by both. Pure Python ≈ 3 s per Mpx |
| `watershed` via `skimage.segmentation.watershed(A, connectivity=2, watershed_line=True)` | `seaice.core.watershed.watershed_skimage(A, conn)` — **cross-check only, never in a port** | approx | ch05 | seeds with the minima pixels, re-pushes pixels, tags entries with a source label, keeps propagating from line pixels, orders neighbours by distance then row-major → 46–122 differing ridge px on `q.jpg` (46 of 63 ice-interior ridge px on the city-block map) and **inconsistent basin partitions** on 6 of 8 script maps; supersedes the skill's generic `watershed` mapping |
| `BW = imregionalmin(I)`, `imregionalmin(I, conn)`, `imregionalmax(I[, conn])` | `seaice.core.morphology.imregionalmin(I, conn=8) -> bool`, `imregionalmax(I, conn=8)` | exact | ch05 | `skimage.morphology.local_minima/local_maxima(I, connectivity=2\|1, allow_borders=True)` + **constant image → all True** (skimage gives all False; MATLAB all True, also for all-`+Inf`/all-`−Inf` images); NaN → `ValueError` (MATLAB `'nonnan'`); ±Inf fine; logical input as numeric (`imregionalmax(imcomplement(+I))`); `conn` 4/8/cross/`ones(3)`. 126 cases 0 px (37 watershed fixtures × {8,4}, 13 extrema fixtures × 2 × {8,4}: border plateaus, ±Inf, 1×N, 1×1, int16, single, uint8 ties) + the 4 script maps (`Dis_img`: city 4 minima / 18 px, chess 2 / 38, euc 6 / 6, quasi 6 / 10) + `imregionalmin(imimposemin(...)) == marker` |
| `J = imimposemin(I, BW)`, `imimposemin(I, BW, conn)` | `seaice.core.morphology.imimposemin(I, BW, conn=8)` (dtype preserved) | exact | ch05 | R2025a `imimposemin.m` l. 82–126 on `imreconstruct`: `fm = −Inf` on markers / `+Inf` elsewhere (`intmin`/`intmax` for integers), `h = 0.001·(max − min)` for single/double (`0.1` if constant), `h = 1` for integers, `g = min(I + h, fm)` **computed in the input class** (single stays single → bit-identical; float64 arithmetic was 4.8e-7 off), `J = imcomplement(imreconstruct(imcomplement(fm), imcomplement(g), conn))`. Markers must be genuine `−Inf` (a finite sentinel changes all 328 marker px). ±Inf images: `h = Inf`, `−Inf + Inf = NaN`, `min(NaN, fm) = fm` — MATLAB identical → `np.errstate(invalid='ignore')`. 88 cases bit-identical (uint8 saturating, int16 at intmax, single, negative single, double, constant, ±Inf images × 4 marker sets × {8,4}; `marker_watershed.m` `Dimp` 0.0). MATLAB accepts NaN silently, port raises; logical `I` rejected by both. Supersedes the skill's `impose_minima` placeholder |
| Book §5.1.3 Steps 1–2 `(g + 1) ∧ f`, `R^E_{(g+1)∧f}(f)` (text form of minima imposition) | `seaice.ch05_watershed.impose_minima_book(g, markers, conn=8, marker_value=None)` | reimplemented | ch05 | literal `+1` offset (MATLAB's `h` differs) with `f` = `marker_value` on markers (default `floor(min g) − 1` for negative segmentation functions, the book's 0 otherwise) and `+Inf` elsewhere; `watershed` of the result equals `watershed(imimposemin(...))` on the two-disc map |
| Eq. (5.2) `M = R^E_I(I + 1) − I` (regional minima by reconstruction, text only) | `seaice.ch05_watershed.regional_minima_by_reconstruction(I, conn=8) -> bool` | reimplemented | ch05 | `reconstruct_by_erosion(I + 1, I) − I > 0`; constant / all-Inf images special-cased to all-True (`Inf − Inf`); equals `imregionalmin` (and MATLAB) on 13 extrema + 12 plateau fixtures × {8,4} |
| Eqs. (5.1), (5.3)–(5.8) immersion watershed with 3×3 dams (text only, Fig. 5.4) | `seaice.ch05_watershed.watershed_immersion(I, conn=8)`, `threshold_set(I, h)` | reimplemented | ch05 | teaching form: levels from `np.unique(I)`, three-case rule, dams by constrained dilation; identical dams/partition to `watershed` on 1-D profiles, may place dams differently on shared plateaus (agreement ≥ 0.75) — never use in a pipeline |
| `direct_watershed.m` (`watershed(uint8 gray)`, `im(bgm) = 0`, `bwlabel(im)` of a uint8 image) | `seaice.ch05_watershed.direct_watershed(gray) -> DirectWatershed`; `scripts/ch05_direct_watershed.py` | exact | ch05 | 160 basins (MATLAB uint8), 2132 ridge px, overlay and `bwlabel(nonzero)` 0 px; `label2rgb` colours display-only |
| `distance_propagation.m` (`img = ~point; imgDist = −bwdist(~img, 'cityblock'); dist0 = imgDist + \|min\|; dist = uint8(dist0·255/max)`) | `seaice.ch05_watershed.inverse_distance_map(bw, metric) -> DistancePropagation(imgDist, dist0, dist)`; `scripts/ch05_distance_propagation.py --source point\|q.jpg --metric …` | exact | ch05 | single arithmetic; `uint8()` = `to_uint8_saturating` (0 levels); `imcontour` level count display-only (`plotting.contour_overlay`) |
| `distance_watershed.m` (`imgDist = −bwdist(~img, metric); Dis_img = imregionalmin; dis = Dis_img .* bw; [p,q] = find(dis == 1); bw0(find(dis==1)) = 0; imgLabel = watershed(imgDist); img(imgLabel == 0) = 0; bwareaopen(img, 5); bwlabel`) | `seaice.ch05_watershed.distance_watershed(bw, metric='chessboard', min_area=5) -> DistanceWatershed`, `otsu_mask(rgb)`, `inverse_distance(bw, metric)`, `_find_rows_cols`; `scripts/ch05_distance_watershed.py --metric chessboard\|cityblock\|euclidean\|quasi-euclidean` | exact | ch05 | all arrays 0 px for the 4 metrics incl. label values (chess 2 / city 4 / euc 6 / quasi 6 basins; ridge 143 / 210 / 231 / 231 px); `logical .* logical` → double; `find` order column-major |
| `gradients_watershed.m` (`hy = fspecial('sobel'); hx = hy'; imfilter(double(I), h, 'replicate'); g = sqrt(Ix² + Iy²)`, `watershed(g)`, `imclose(imopen(g, ones(7,7)), ones(7,7))`) | `seaice.ch05_watershed.sobel_magnitude(gray)`, `gradient_watershed(gray, smooth=7) -> GradientWatershed`; `scripts/ch05_gradients_watershed.py` | exact | ch05 | **unscaled** Sobel on the 0–255 double image (no `/8`, no `/255` — not `edge`'s quantity), `g`/`g2` ≤ 1e-12; 350 basins (MATLAB **uint16**) / 12 basins, ridges and overlays 0 px; bool `ones((7,7))` SE takes `imclose`'s zero pre-pad path |
| `marker_watershed.m` (`imregionalmin` → `imdilate(…, strel('disk', 5))` → `imimposemin` → `watershed` → `bwareaopen(…, 5)`) + commented centroid block | `seaice.ch05_watershed.marker_watershed(bw, metric='cityblock', radius=5, point_markers=False, min_area=5) -> MarkerWatershed`; `scripts/ch05_marker_watershed.py [--centroid-markers]` | exact | ch05 | 4 minima / 18 px → 2 marker components (octagon disk 9×9/69) → imposed map bit-identical in single → 2 basins (140 ridge px) → 2 floes; `point_markers=True` = the un-commented block (`marker0` 2 px, 142 ridge px, 2 floes) also 0 px |
| `regionprops(bwlabel(BW), 'Centroid')`, `cat(1, cen.Centroid)`, `floor(...)` | `seaice.ch05_watershed.component_centroids(mask, conn=8) -> (n, 2)` 1-based `(x, y)` | exact | ch05 | mean of (col + 1, row + 1) per `label_components` component in `bwlabel` order; `[23.3089 68.3591; 46 50]` on the marker image and `allclose` on a random 20×30 fixture; `floor` → 1-based integer pixel (subtract 1 to index). **Since ch06 this is a thin wrapper over `seaice.core.regionprops.regionprops(L, 'Centroid')`** (rule 9; ch06 review S8) — same signature, same `bwlabel` ordering, one code path. `core.regionprops` also covers Area/BoundingBox/ConvexHull/ConvexImage/ConvexArea/Solidity/Major-MinorAxisLength/Eccentricity/Orientation/Perimeter/PixelIdxList/PixelList/Image |
| `topological_surface.m` (`imcomplement(uint8)`, Sobel magnitude, `−bwdist(~im2bw(gray, graythresh(gray)), 'chessboard')`, `surf(...,'FaceColor','texturemap','EdgeColor','none'); colormap(copper); xlim([0 85])`) | `seaice.ch05_watershed.topographic_surfaces(rgb) -> dict`, `seaice.core.plotting.surface_plot(Z, path, cmap='copper', xlim=(0, 85))`; `scripts/ch05_topological_surface.py` | exact (arrays) / display (surface) | ch05 | `x = 255 − I` 0 px, `g` ≤ 1e-12, gray-threshold mask (128/255) 0 px, `d` 0.0 (single); matplotlib `plot_surface` viewpoint differs from MATLAB's |
| `chaincode_corner.m` / `watershed_based/freeman_concave.m` (Eqs. 5.9–5.18 loops: `R = mod(C(i) − C(i−1) + 8, 8)`, `R > 4 → R − 8`, `A`, `Sum` with `+16`/`+8`, `Sum − Sum0`, `D(i) = S(i+3) − S(i)`, wrap `S(3−j) − 24 − S(N−j)`, `Diff = [D0, D(1..l−1)]`, `p = find(Diff >= 3 & Diff <= 10)`) | `seaice.ch05_watershed.differential_chain_code(fcc) -> (R, A, S, D, Diff)`, `freeman_concave(I, object='first', lo=3, hi=10) -> ConcaveResult`; `scripts/ch05_chaincode_corner.py` | exact | ch05 | integer arithmetic identical on the 262-code boundary of `q.jpg` (`A(N) = −8`, `S(N) = −24`, `Diff ∈ [−5, 7]`, 9 concave points at the two notches) and per line of `main.m` (1 / 1 / 9 points; crop 8 / 1 / 1 / 1 / 8 / 14 / 15). `im2bw(I, graythresh(I))` on the RGB (all-plane Otsu) or on a `{0,1}` double region (→ `I > 0`); `boundaries(B, 8, 'cww')` = clockwise; **`b{1}` = first `bwlabel` object** (the `[max_d, k]` it computes is unused → `object='longest'` opt-in); raises where MATLAB errors (`A(l−2)` on < 3 codes) |
| `main.m` Step 4 ending points: `imfilter(double(g), [0 −1 0; −1 4 −1; 0 −1 0])` (zero pad), `abs(...) >= max(...)`; text rule "≥ 3" | `seaice.ch05_watershed.junction_endpoints(mask, rule='max'\|'ge3') -> (n, 2)` 0-based in `find` order, `ENDPOINT_KERNEL` (= `synth.FIG_5_15_KERNEL`) | exact (`'max'` = script; `'ge3'` = text) | ch05 | line pixel responds `4 − #line 4-neighbours`, background `−#line 4-neighbours`; 12 Fig. 5.15 patterns → 3, isolated pixel → 4, closed 4×4 loop → max 2 and `abs >= 2` hits 16 px incl. 4 enclosed background px (MATLAB identical); `'ge3'` is the **signed** `>= 3` (no `abs`, never background). Same 6 endpoints on `q.jpg` (`T = 3` per line); single-pixel lines give `T = 4` and one ending point |
| `main.m` Steps 2–5 (`D = bwdist(~bw,'cityblock'); L = watershed(−D); w = L == 0; f = bitand(bw, w); seg = bw & ~w; [label, num] = bwlabel(f, 4)`; per line: `neighbor = seg; neighbor(p) = 1; connect = bwlabel(neighbor); im = imreconstruct(g, connect); concave = freeman_concave(im); c = intersect(ep, concave, 'rows'); if numel(c) == 0, seg(p) = 1`) | `seaice.ch05_watershed.neighboring_region_merging(bw, metric='cityblock', endpoint_rule='max', sequential=True) -> MergeResult(D, L, w, f, seg0, seg, label, num, lines: list[JunctionLine])`, `_intersect_rows`; `scripts/ch05_main.py [--endpoint-rule ge3] [--no-sequential]` | exact | ch05 | `q.jpg`: 4 basins, 3 lines (22/22/19 px), `REMOVED = [1 1 0]`, 4 → 2 floes, per-iteration `seg` 0 px; synthetic image 1 line kept; 552×574 Fig. 4.3(a) crop 11 basins, 7 lines, `REMOVED = [0 1 1 1 1 0 0]`, 11 → 7 floes — all per-line records identical. `bitand(logical, logical)` is logical; `bwlabel(f, 4)` because 8-conn watershed lines are 4-connected; **`seg` is updated inside the loop** (`sequential=True` = script; `False` = the text's description); `intersect(...,'rows')` = sorted unique common rows |
| `nrm_junction_ending.fig` (authors' saved Fig. 5.14(f); MAT v5 `hgS_070000`) | `openfig(..., 'invisible')` in `make_refs.py` → `fig_5_14f.mat` (image `CData`, line series); Python: `scipy.io.loadmat(struct_as_record=False)` | exact (L3 truth) | ch05 | `CData` (96×81 logical) = `f` 0 px; the 3 line series' end points = the port's 6 ending points; R2025a re-creates no text objects from the old file. Shipped `.fig` files are free references — read them |
| `bitand(BW1, BW2)` on logicals; `BW1 .* BW2`; `intersect(A, B, 'rows')`; `[p, q] = find(BW)` | `bw1 & bw2` (bool); `(bw1 & bw2).astype(float)` when the class matters; `ch05_watershed._intersect_rows`; `_find_rows_cols` | exact | ch05 | `class(bitand(true, false))` = logical; `logical .* logical` = double (`dis = Dis_img .* bw`); `intersect` rows sorted/unique, empty `0×2`; `find` column-major |
| `label2rgb(L, 'jet', 'k', 'shuffle')` and `label2rgb(index, @jet, [1 1 1])` (**extended in ch07**) | `seaice.core.plotting.label2rgb(L, cmap='jet', background='k'\|(1,1,1), shuffle=True\|False, seed=0) -> uint8 RGB` | display only | ch05, **extended ch07** | MATLAB samples the colormap at `max(L)` levels and permutes with a private fixed-seed `RandStream` — deterministic there, not reproducible here; never compare colour images. Supersedes the skill's `skimage.color.label2rgb` mapping for this idiom. **ch07 extension (not a second function, rule 9):** `ice_shape_enhancement.m` l. 183 passes an *index* image whose values are Eq. (7.6) colours up to ~10⁴, not 1..N labels, with a function-handle colormap and an **RGB-triple** background → MATLAB builds a 10 000-entry `jet` LUT. Confirmed not to blow up; the **index arrays** are 0 px vs MATLAB and the rendered maps are visually identical — the PNGs are deliberately not compared |
| `imcontour(Z)`, `image(dist, 'CDataMapping', 'scaled'); hold all` | `seaice.core.plotting.contour_overlay(img, Z, path, levels=10)` | display only | ch05 | `imcontour`'s automatic level count is not reproduced; axes 1-based pixel centres |
| `surf(double(Z)); set(h, 'FaceColor', 'texturemap', 'EdgeColor', 'none'); colormap(copper); xlim([0 85])` | `seaice.core.plotting.surface_plot(Z, path, cmap='copper', xlim=(0, 85), elev=30, azim=−60)` | display only | ch05 | matplotlib `plot_surface`; x = column (1-based), y = row |
| `q.jpg` toy image (96×81 RGB, two touching floes), `strel('disk',5)` marker recipe, Fig. 5.15 patterns/kernel, Fig. 5.16 matrix, watershed tie fixtures | `seaice.core.synth.two_touching_floes(shape=(96, 81), seed=0, noise=6.0)`, `plateau_fixtures()`, `FIG_5_15_ENDPOINT_PATTERNS`, `FIG_5_15_KERNEL`, `FIG_5_16_IMAGE`, `FIG_5_16_TRACE_MATLAB` | exact (printed truths) / reimplemented (fixtures) | ch05 | `main.m` on the synthetic image is exact vs MATLAB (`main_synth.mat`); Fig. 5.16's `(2,3) → (2,4) → (3,5)` is a contiguous sub-sequence of the DIPUM trace starting at `(3,2)`; `plateau_fixtures` are int16 (cast to double for MATLAB `watershed`) |
| `del2(f)`, `del2(f, h)`, `del2(f, hx, hy)` | `seaice.core.matlab_compat.del2(f, hx=1.0, hy=None)` | exact | ch06 | **not** the Laplacian: `∇²/(2·ndims)` (`∇²/4` in 2-D — `GVF.m` writes `mu*4*del2(u)` to restore Eq. 6.52c), borders **linearly extrapolated** (`g(1) = g(2)(h1+h2)/h2 − g(3)h1/h2`; unit spacing `2g(2) − g(3)`), `n == 3` copies, `n ≤ 2` → 0, and it divides by `ndims(f)` even in the 1-D branch; `del2(f, h)` uses `h` for both dims, `del2(f, hx, hy)` swaps them (`x` = dim 2). 21 cases 0.0 incl. ±Inf (NaN patterns identical, run under `np.errstate(invalid='ignore')`) |
| One MATLAB **integer-class** arithmetic operation (`uint8 − uint8`, `uint8 ./ uint8`, …) | `seaice.core.matlab_compat.saturate_to_class(x, dtype)` | exact | ch06 | round half away from zero **then** saturate, returned as float64 carrying the integer values (what MATLAB produces once the result lands in a `double` array). Every step of a chain MATLAB keeps in an integer class must be pushed through separately — the intermediate values are *not* the float64 ones |
| `BoundMirrorExpand(A)` / `BoundMirrorEnsure(A)` / `BoundMirrorShrink(A)` (Xu & Prince) | `seaice.core.snake.bound_mirror_expand / bound_mirror_ensure / bound_mirror_shrink` | exact | ch06 | Eq. 6.46 natural BC (zero normal derivative); `expand` == `np.pad(A, 1, 'reflect')`; `ensure` reproduces MATLAB's error text for a 2×2; the port additionally raises for `m < 2 or n < 2` where MATLAB runs on a 1×N (documented) |
| `[u, v] = GVF(f, mu, ITER)` (Xu & Prince) | `seaice.core.snake.gvf(f, mu, iters, *, check_cfl=True)` | exact | ch06 | Eqs. 6.41/6.50–6.55; **normalises `f = (f − fmin)/(fmax − fmin)` in the INPUT's class** — a uint8 image gives a {0,1} edge map (review M1, a proven defect before it was reproduced); `mu*4*del2` + the mirror BC; constant input → NaN like MATLAB; refuses `μ > 0.25` at unit steps (CFL). 44 cases 0.0 incl. ITER 0/1, 3×3, 3×5, ±Inf, both book images |
| `gradient2(a)`, `[gx, gy] = gradient2(a, hx, hy)` (MATLAB-4 `gradient`, renamed by the authors) | `seaice.core.snake.gradient2(a, xax=1.0, yax=1.0)`, `gradient2_complex`, `gradient2_magnitude` | exact | ch06 | central differences, one-sided at the ends = modern `gradient`/`np.gradient[::-1]`; **differences the input in its own class** (integers saturate/round). MATLAB **cannot run it on an integer image** (line 47 divides by a double *matrix*: `Integers can only be combined with integers of the same class, or scalar doubles`) — the port accepts it (documented superset, L1-only) |
| `xconv2(I, G)` (Xu & Prince FFT convolution) | `seaice.core.snake.xconv2(I, G)` | exact | ch06 | `fftconvolve` + the authors' `floor(n1/2)` crop; ≤ 9.1e-13 vs MATLAB and ≤ 6.8e-13 vs `conv2(…,'same')` (the header promises 1e-10); an even mask exercises the crop |
| `gaussianMask(k, s)`, `gaussianBlur(I, s)` (Xu & Prince) | `seaice.core.snake.gaussian_mask(k, s)`, `gaussian_blur(I, s)` | exact | ch06 | Eqs. 6.10/6.13; `R = ceil(3s)` → `(2R+1)²`, **not** `fspecial('gaussian')`; ≤ 1.4e-20 / 6.4e-14 (uint8 input too). Never executed by the ch6 drivers (σ = 0) but used for Figs. 6.4(b)/6.7(b) |
| `snakedeform(x, y, alpha, beta, gamma, kappa, fx, fy, ITER)`; its internal `A`, `invAI = inv(A + gamma*eye(N))` | `seaice.core.snake.snakedeform(..., *, book_index=False, solver='auto')`, `snake_matrix(N, alpha, beta, *, book_index=False)`, `snake_first_column(...)` | exact (dense) / near (circulant, default `N ≥ CIRCULANT_MIN_N = 32`) | ch06 | Eqs. 6.34–6.40; `A` 0.0 for 12 (N, α, β) incl. N = 3; `inv(A+γI)` ≤ 3.3e-16; deform ≤ 1.8e-13 dense, ≤ 4.1e-13 circulant. With constant α, β, `A + γI` is a **symmetric circulant** → FFT solve, **306 s → 2.1 s** on `alg_seg_gray.jpg`, ≤ 8.4e-12 apart, final curve 2.7e-8 px, burnt mask 0 px. Uses `core.interp.interp2(fx, u=y−1, v=x−1, 'bilinear', fill=0.0)` = `interp2(fx, x, y, '*linear', 0)` (0.0 in and out of range) |
| `snakeinterp(x, y, dmax, dmin)`, `snakeindex(idx)` (Xu & Prince) | `seaice.core.snake.snakeinterp(x, y, dmax, dmin, *, max_passes=100)`, `snakeindex(idx)` | exact | ch06 | city-block spacing, `[x; x(1)]` wrap; 16 of 18 cases 0.0 — the other 2 are the header's own admitted removal bug where **MATLAB errors** (`Index exceeds array bounds`) and the port raises with a diagnostic; `max_passes` is our cap on the unbounded `while max(d) > dmax` loop. **Point counts are unstable at the 1e-13 level** on 500–2800-point contours (hundreds of spacings sit exactly at `dmax`): compare the curve (Hausdorff) and the mask, not the length |
| `snakedisp(x, y, style)` | `seaice.core.plotting.snake_plot(ax, x, y, style)`; GVF quiver → `quiver_field(ax, u, v, step, scale)` | display only | ch06 | closes the curve with `[x, x(1)]` |
| `regionprops(L \| BW, 'Area','Centroid','BoundingBox','ConvexHull','ConvexImage','ConvexArea','Solidity','MajorAxisLength','MinorAxisLength','Eccentricity','Orientation','Perimeter','PixelList','PixelIdxList','Image','FilledImage','FilledArea')` | **`seaice.core.regionprops.regionprops(L_or_bw, properties=None, conn=8) -> list[RegionProps]`**, `region_table(...)` | exact | ch06 | **never `skimage.measure.regionprops`** (10.2935 vs 10.3581 axis length, 26.83 vs 25.42 perimeter, orientation +90° → the `Rc = 0.9`/`Rl = 2` decisions move). MATLAB's algorithms: second moments **+ 1/12**, `y` negated, `uyy > uxx` branch, `Orientation` in degrees; `ConvexHull` = hull of the **mid-edge points** of the `bwmorph('perim8')` pixels via `convhull(rr, cc)` (that argument order); `ConvexImage` = `roipoly(M, N, c, r)` with the `firstRow/firstCol` shift; `Perimeter` = Vossepoel–Smeulders `0.980·even + 1.406·odd − 0.091·corners` on a Moore trace of the 8-conn boundary. ≤ **1.07e-14** on 40 shapes × 10 properties and on all **344** components of three real masks, in both the label-matrix and the `regionprops(label == n, …)` loop form. `Centroid` = 1-based `(x, y)`; `BoundingBox` = `[x y w h]` with 0.5 offsets |
| `regionprops(...,'ConvexHull')` vertex list | same | near | ch06 | MATLAB's `convhull` **keeps collinear hull points** (30 vertices where the port has 8 on an L shape); the port's list is a subset and the polygon is identical → `ConvexImage`/`ConvexArea`/`Solidity` are 0 px / exact |
| `poly2mask(x, y, M, N)`, `roipoly(I, xi, yi)` / `roipoly(M, N, xi, yi)` (the **non-interactive** 3-/4-argument forms) | `seaice.core.polygon.poly2mask(x, y, M, N)`, `roipoly(m, n=None, xi=None, yi=None)` | exact | ch06 | line-by-line port of `eml/poly2mask.m` (5× sub-pixel snapping, `intLine` edge walk, per-column parity scan); **0 px** on 19 masks incl. the `regionprops` hulls. Supersedes the skill's "`skimage.draw.polygon2mask` → near" |
| `polybool('intersection', [x1 x2 x2 x1], [y1 y1 y2 y2], x, y)` (Mapping TB, compiled GPC `gpcmex`) | `seaice.core.polygon.clip_polygon_rect(x, y, x_range, y_range)` (Sutherland–Hodgman, returns a **closed** ring) | reimplemented | ch06 | vertex **sets**, point counts and rasterised masks identical on 7 fixtures and on 46/46 per-seed clips; the **ordering is not reproducible** — GPC rotates the start vertex and reverses the traversal (0 of 46 clips start at max-y/min-y/max-x/min-x or either lexicographic extreme). It returns a closed ring and with `Dmin = 0` the duplicate survives `snakeinterp` (MATLAB's own `for_test.m` reference: 252 points vs the open ring's 251). Compare sets/masks, never order; a rotated start vertex shifts `snakeindex`'s insertion parity by one (rotating the port's own ring moves the burnt mask 56–82 px, reversing it 0 px) |
| `polyarea(x, y)` | `seaice.core.polygon.polyarea(x, y)` | exact | ch06 | shoelace; ≤ 1.85e-13 on 20 polygons × {open, closed} incl. a clockwise copy, a self-intersecting bowtie (0 on both sides) and collinear/duplicate/2-point/1-point degenerates; repeating the first vertex changes nothing |
| `convhull(x, y)` | `seaice.core.polygon.convhull(x, y, simplify=True)` | near | ch06 | same hull **vertex set**, closed; start vertex differs and MATLAB retains collinear edge points. R2025a **rejects** the 2007 option list (`convhull(x, y, {'Qt'})` → *CONVHULL no longer supports or requires Qhull-specific options*) and **errors** on collinear/duplicate clouds where the port returns a degenerate result |
| `[xi, yi, ii] = polyxpoly(x1, y1, x2, y2)` (Mapping TB) | `seaice.core.polygon.polyxpoly(x1, y1, x2, y2)` | reimplemented | ch06 | crossing squares: the 2 intersection points and their segment indices identical (**0-based** here); disjoint case empty on both |
| `polygeom(x, y) -> [geom, iner, cpmo]` (H. J. Sommer III, FEX) | `seaice.core.polygon.polygeom(x, y)` | near | ch06 | `geom = [A, x_cen, y_cen, P]` ≤ 1e-12, `iner` ≤ 7.3e-12, principal moments ≤ 1.4e-12; **`ang1`/`ang2` differ from R2025a's `eig` by exactly ±π in 13 of 15 polygons** (an eigenvector's sign is arbitrary; the axes are identical). The port's convention reproduces the M-file's own self-test (area 15, centroid (3.415, 6.549), perimeter 16, `I1` 11.249, **ang1 = 30°**); R2025a running the shipped file prints −150° |
| `minboundrect(x, y, metric)` (John D'Errico, FEX) | `seaice.core.polygon.minboundrect(x, y, metric='a'\|'p')` | exact | ch06 | rotating calipers over `unique(mod(edgeangles, pi/2))`; `rectx`/`recty` ≤ 1.8e-15, `area`/`perimeter` ≤ 1e-12 incl. the `nedges` 1 and 2 cases. **The shipped file does not run in R2025a** (its `convhull(x,y,{'Qt'})`); the reference dropped `{'Qt'}` and nothing else. The port is a superset: MATLAB returns `[]` for `nedges = 0` and a 1×2 perimeter for `nedges = 2`. This is **ch9 §9.3**'s rectangularization and the book's own form of ch9 p. 205 criterion 3 |
| `bwperim(BW)` / `bwperim(BW, 8)` / `bwmorph(BW, 'perim8')` | `seaice.core.connectivity.bwperim(bw, conn=4)` | exact | ch06 | pad 0 → `imerode` with the connectivity nhood → `b & ~eroded` → crop; 9 shapes × conn {4, 8} 0 px; image-border pixels are perimeter pixels. **Supersedes `connectivity.region_boundary_mask`** (ch02, text-only) for any MATLAB port |
| Eqs. (6.57) `M_max = I − R^D_I(I − 1)` / (6.58) `M_max = I + 1 − R^D_{I+1}(I)` (text only) | `seaice.core.morphology.regional_maxima_by_reconstruction(I, conn=8, form='6.57'\|'6.58')` | reimplemented | ch06 | dual of ch05's Eq. 5.2 form; equals `imregionalmax` 0 px on every case tried (city-block and Euclidean distance maps, synthetic floe field), including the constant-image and `+Inf` special cases |
| Statistics TB `kmeans(double(I(:)), k, 'EmptyAction','singleton')` (no shadowing `kmeans.m` in ch6/ch7/ch9) | `seaice.core.clustering.kmeans_lloyd(X, k, init='kmeans++', seed=0)` | approx | ch06 | R2025a defaults `sqeuclidean` / `Start 'plus'` / `Replicates 1` / `MaxIter 100` = the same algorithm, different RNG stream. Measured vs MATLAB under `rng(0)`: sorted centres `[107.872949, 152.264015, 198.773459]` **equal to double precision** and the mask `bk` **0 px** on 40 602 and on 408 972 pixels — but 20 restarts with other seeds show the optimum is **not unique** (per-centre spread 2.20 / 2.00 / 0.09 gray levels), so compare sorted centres + pixel agreement, never labels. **Not** ch3's `kmeans_gray` (that shadows the toolbox only in `ch3/`) |
| `homofil(im, d, n)` (orphan; no caller, no book section) | `seaice.core.filters.homomorphic_butterworth(im, d, n=1, *, alpha_l=0.0999, alpha_h=1.01, matlab_bug=True)` | exact | ch06 | ≤ 1.14e-12 on 6 cases (2 images × `(d, n)` ∈ {(10,1), (30,2), (5,4)}), uint8 too. `H = 1/(1 + (d/A)^{2n})`, `A(i,j) = sqrt((i − r/2)² + (j − c/2)²)`, `H ← 1 − ((α_H − α_L)H + α_L)`, applied to `log2(1 + im)`, result `exp(\|ifft2(H·fft2(...))\|)`; the typo'd variable and the missing `fftshift` are behind `matlab_bug=True` (the default, = the file) |
| `GVF_distance.m` (§6.3.3 + §6.4 Algorithm 1; also called by ch9 `model_ice_demo.m`) | `seaice.ch06_gvf_snake.gvf_distance(I, *, sigma, GradientOn, GVFOn, Num, mu, iter, alpha, beta, gamma, kappa, Dmin, Dmax, Ra_min, Ra, Rc, Rl, se_radius, timer, …) -> GVFDistance`; `scripts/ch06_gvf_distance.py` | near | ch06 | every array 0.0 / 0 px up to the snakes (`gray`, `level`, `bw`, `f2`, `u`, `v`, `px`, `py`, `label`/`num`, criteria ≤ 1e-9, the selection `k`, `bw2`, `img_Dist`, `Dis_img`, dilated `dis`, `label1`/`num1`, centroids ≤ 1e-12, **radii bit-exact**, all 46 `snakeinterp` outputs, all 46 clips); the final mask `bw1` differs by **16 of 31 730 ice px (0.050 %)** (14 with the default solver), same burnt-pixel count 1220, curves Hausdorff ≤ **0.4794 px** — all of it `polybool`'s start-vertex rotation. MATLAB 430 s, port 23 s |
| `seaice_kmean_GVF_forenhancement.m` | `seaice.ch06_gvf_snake.seaice_kmean_gvf(I, *, kms0=3, …, kmeans_impl='lloyd++', strict_bwareaopen=False) -> KmeanGVF` | near | ch06 | `bw`, `f2`, `px`, `py` 0.0; `bk` 0 px of 40 602; `bw0` after `bwareaopen(…, 10, 4)` 0 px; pass-2 `bw0` 4 px; the three-level `out = bw1 + 0.5·bw0` has exactly the levels {0, 0.5, 1} on both sides and differs on **0.049 %** of pixels. `bw_kmeans − bw` had **0** negative pixels on both book images, so the `bwareaopen`-on-a-signed-double quirk (`strict_bwareaopen=True`, `KmeanGVF.n_negative`) is latent and untested |
| `sea_ice_demo.m` (the chapter's only driver) | `scripts/ch06_sea_ice_demo.py` (`--full` raises `NotImplementedError`, `--downscale`, `--max-seeds`) | near | ch06 | parameter block asserted value-by-value; every stage on the full 1038×394 `sea_ice_test.jpg` verified against MATLAB (231 components, 598 seeds, k-means stage 0 px); the `ice_shape_enhancement` (**ch7 §7.1**) / `sea_ice_model` (**ch8 §8.2**) tail is deferred |
| `for test/dist.m` (§6.3.3 seeds and initial circles; reads its image by an absolute path) | `scripts/ch06_dist.py` (via `ch06_gvf_snake.initialize_contours`) | exact | ch06 | in the script's transposed frame every array is 0 px, `[p, q] = find` matches MATLAB's column-major order (2616 points), **540 seeds**, **all 540 radii bit-exact**, 536/540 circles bit-exact (worst 5.68e-14), 6 seeds take the `r == 0 → 2` fallback on both sides. A copy must be renamed before running in MATLAB — `dist.m` **shadows the Deep Learning Toolbox `dist`** |
| `for test/for_test.m` (§6.1.2/§6.2 single-snake demo) | `scripts/ch06_for_test.py` | exact (mask) / near (point order) | ch06 | `bw`, `f2`, `u`, `v`, `px`, `py` 0.0; the 64×64 quiver grid ≤ 1e-12 **including the authors' swapped axes** (`ySpace = 1:148/64:148` indexes a 108-row field, so **1088 of 4096 samples (26.6 %) are NaN in MATLAB's own `interp2` too**, identical NaN patterns); clip = closed 127-point ring with MATLAB's vertex set; fed MATLAB's own clip the port reproduces all 10 five-iteration blocks ≤ 1.1e-7; from its own ring 778 points vs 777, **7.2e-4 px** apart, burnt mask **0 px** (91 px burnt on both sides). Note `[v, h] = size(bw)` **shadows the GVF field `v`** at line 119 |
| §6.3.3 / Algorithm 1 contour initialisation (`bwdist(~bw,'cityblock')`, `imgDist(~bw) = −inf`, `imregionalmin`, `.* bw`, `imdilate(strel('disk',3))`, `bwlabel(dis,8)`, `r = D(round(cy), round(cx))/√2`, `r == 0 → 2`, `t = 0:0.05:6.28`) | `seaice.ch06_gvf_snake.initialize_contours(bw, *, metric='cityblock', se_radius=3, radius_divisor=√2, min_radius=2.0, form='script'\|'book')` | exact (script) / reimplemented (book form) | ch06 | `strel('disk', 3)` is **5×5 / 25 px** (not 7×7/37 — the only printed evidence for `T_seed`); `t` gives **126 points**, last 6.25, so the circle is **not** closed; the `book` form (Eq. 6.57 regional maxima) gives the same maxima set on Figs. 6.14/6.16 |
| ch9 p. 205 re-segmentation criteria (`a > Ra`, `rc < Rc`, `l/w > Rl`, `k = unique([...])`) | `seaice.ch06_gvf_snake.component_criteria(bw, Ra, Rc, Rl, conn=4)` | exact | ch06 | criterion 2 = `Solidity` (the convex hull *is* the minimum-area bounding polygon); **criterion 3 is written in the book as the min-area bounding rectangle ratio but coded as the ellipse axis ratio** — `core.polygon.minboundrect` computes the book's version. `k` 0-based and sorted (MATLAB `unique`) |
| Eqs. 6.6–6.13 external energies, Eq. 6.29 traditional snake (text only, no `.m`) | `seaice.ch06_gvf_snake.{line_energy, edge_energy, termination_energy, external_energy, traditional_snake}` | reimplemented | ch06 | the U-shape experiment reproduces the §6.1.3 vs §6.2 contrast; note that at the Fig. 6.7(b) caption's σ = 4 the *traditional* snake also enters the concavity (0 % penetration at σ = 1, 2 — the Gaussian is what buys the capture range), so the honest figure is a σ sweep |
| Fig. 6.14 printed 8×8 matrices; Fig. 6.16's 110×186 fixture; the U-shape; a synthetic floe field | `seaice.core.synth.FIG_6_14_IMAGE`, `FIG_6_14_DISTANCE`, `fig_6_16_circles(shape=(110,186), 61, 9)`, `u_shape(...)`, `synthetic_floe_field(...)` | exact (printed truths) / reimplemented (fixtures) | ch06 | `bwdist(~FIG_6_14_IMAGE,'cityblock')` == the printed matrix 0 px and == MATLAB 0.0; the Fig. 6.16 fixture is exactly the text's specification |
| `imread` of an image inside a **sub-folder** of the chapter's MATLAB directory | `seaice.core.io.load_image(chapter, name)` (`resolve_case_insensitive` now recursive: top level first, then sub-directories case-insensitively in sorted order) | exact | ch06 | ch06 is the first chapter whose book images live in `Sea_Ice_Floe_Identification/` and `for test/` (with a space); a Colab reader copies that tree verbatim. `io._fetch_non_blank` also rejects a **zero-variance** public-domain download and retries once (a 389-byte blank JPEG was observed once from NASA Worldview) |
| `imfill(I, 'holes')` / `imfill(BW, 'hole')` / `imfill(I, conn, 'holes')` | `seaice.core.morphology.imfill(I, mode='holes', *, conn=4)` → **same dtype as `I`** | exact | ch07 | line-by-line port of R2025a `imfill.m` l. 124–145: `uint8(I)` if logical → `padarray(·, 1, -Inf, 'both')` → `imcomplement` → marker with the interior set to `-Inf` → `imreconstruct(marker, mask, conn)` → `imcomplement` → crop → `logical()` if the input was. **21 constructed cases × conn {4, 8} = 42 comparisons, all 0 px, and the output class matches on all 21** (logical/double/uint8/single/int16, nested holes, border-open holes, 8-connected rings, all-true/all-false, grayscale surfaces, `±Inf`). **Supersedes the skill's "`scipy.ndimage.binary_fill_holes` — exact"**: the library call matches MATLAB only on the **logical conn-4** branch (3 of 13 logical cases differ at conn 8) and never on a numeric input — ch7 passes a **double**, and the returned class is what `bwlabel`/`imclose`/`out(pp) = t` see. Also **Eq. (7.4)** |
| `[counts, centers] = hist(y, n)` / `hist(y, centres_vector)` | `seaice.core.histogram.hist(y, bins=10) -> (counts, centers)` | exact | ch07 | port of `toolbox/matlab/graphics/math/hist.m` l. 81–156. **Centres, not edges**: scalar `n` → `edges = linspace(miny, maxy, n+1)`, `x = edges(1:end-1) + binwidth/2`, with the `min == max` widening `[miny − floor(n/2) − 0.5, maxy + ceil(n/2) − 0.5]`; a **vector** is the bin *centres*, internal edges are their midpoints and **both outer bins are unbounded** (out-of-range values are counted, not dropped). The first comparison edge is `−Inf`, so **`−Inf` counts in the first bin and `+Inf` in the last** — the non-finite guard only removes them from `min`/`max`, and an all-non-finite input sets `miny = maxy = 0`. **23 of 23 cases 0 px** (counts *and* centres) + both empty forms. `np.histogram` reproduces neither rule. Deliberate deviation: raises for `n < 1` (MATLAB's `binwidth = Inf` branch, unreachable from the book) |
| `eps(x)` (as in `hist.m` l. 145 `edgesc = edges + eps(edges)`) | `abs(np.spacing(x))` — **not** `np.nextafter(x, np.inf)` | exact | ch07 | `eps` is the positive spacing at `\|x\|`: on a **negative** value whose magnitude is an exact power of two `eps` steps 2⁻⁵² where `nextafter` steps 2⁻⁵³, a factor of two, and it flips a `hist` bin (`y = −2 + eps(−2)/2`, centres `[−3 −1 1]`: MATLAB `[1 0 0]`, a `nextafter` port `[0 1 0]`). Verified bit-exact against MATLAB's own `eps([−1 −2 −0.5 1 2 0 −3])` |
| `[A, ind] = sort(v)` where the order decides a later overwrite | `np.sort(v)`, `np.argsort(v, kind='stable')` | exact | ch07 | MATLAB's `sort` is **stable**: `sort([4 4 6 4 1 6 1 9 4 6 1 2])` → `ind = [5 7 11 12 1 2 4 9 3 6 10 8]`. Algorithm 2 superimposes pieces small → large, so ties (everywhere among 2–5 px brash pieces) decide which label survives; the port's `order + 1` equals MATLAB's `ind` on all 9 fixtures and on the 1211 pieces of the real image |
| `cleaning & labeling & filling/morphology_cleaning.m` (§7.1.1, Fig. 7.2) | `seaice.ch07_ice_type.morphological_cleaning(I, se) -> MorphologicalCleaning`; `scripts/ch07_morphology_cleaning.py` | exact | ch07 | cleaning = `imopen(imclose(I, se), se)`; `I, f1, f2, f0, c1…c4` all **0 px** vs MATLAB, sums 111 → 115 / 104 / 108. The **2×2 square** SE is even ⇒ asymmetric origin — it exercises `imdilate`'s SE reflection and `imclose`'s 0 pre-pad |
| `cleaning & labeling & filling/labeling.m` — **Eq. (7.1)** `X_k = (X_{k−1} ⊕ B) ∩ A` (Figs. 7.3/7.4) | `seaice.ch07_ice_type.connected_component_extract(A, seed, se='square3'\|'diamond1', max_iter=None)` ; `scripts/ch07_labeling.py --se …` | exact (file) / reimplemented (equation) | ch07 | every printed block 0 px for both SEs (8-conn sums 9, 5, 8, 12, 17, 20, 23, 24 — "completes at the 8th iteration"; 4-conn 5, 4, 7, 9, 12, 13, 14). The port stops at the fixed point and MATLAB's unrolled tail is verified to *be* that fixed point; equals the `bwlabel` component containing the seed for both connectivities |
| `cleaning & labeling & filling/filling.m` — **Eq. (7.2)** on `A^c` (Figs. 7.5–7.7) | `seaice.ch07_ice_type.hole_fill_dilation(A, seed, se='diamond1', max_iter=None) -> HoleFillDilation`; `scripts/ch07_filling.py --image fig75\|fig76` | exact (file) / reimplemented (equation) | ch07 | Fig. 7.5 8/8 blocks, Fig. 7.6 4/4, Fig. 7.7 10/10 vs MATLAB. The cross SE provably misses a second 4-connected hole and the 3×3 square provably floods the whole complement. Fig. 7.6/7.7's input (Fig. 7.5's image **+ (5,5) 1-based**) is in **no** shipped `.m` → `core.synth.FIG_7_6_IMAGE`, confirmed by a patched MATLAB run |
| `filling_reconstruct.m` — **Eq. (7.3)** border marker `F_m` | `seaice.ch07_ice_type.border_marker(F)` | exact | ch07 | `1 − F` on the border, 0 inside; **equals the script's hand-written `x0` literal** in all three variants (27 px on Fig. 7.8) |
| `filling_reconstruct.m` — **Eq. (7.4)** `H = [R^D_{F^c}(F_m)]^c` (Fig. 7.8) | `seaice.ch07_ice_type.hole_fill_reconstruct(F, conn=4)`; `scripts/ch07_filling_reconstruct.py`; library form = `core.morphology.imfill` | exact | ch07 | 3 variants (shipped `I0` × cross, × `square,3`, the file's **commented alternative** `I0` × cross): `I, x0, x10 = H, I1, I2` all 0 px; sums 39 → 48 → 9 filled px. `H == imfill(F,'holes') == binary_fill_holes(F)` for **logical** input only |
| `Sea_Ice_Floe_Identification/ice_shape_enhancement.m` (252 l) — **Algorithms 2/4/5, Eqs. (7.5)/(7.6)** | **`seaice.ch07_ice_type.ice_shape_enhancement(bk, seg, min_floe=40, min_brash=1, se_th=50, *, nbins=50, crop=True, book_threshold=False) -> IceShapeEnhancement`** (alias `sea_ice_shape_enhancement` = Algorithm 4); `scripts/ch07_ice_shape_enhancement.py` | exact | ch07 | the chapter's one new algorithm. **9 controlled `(bk, seg)` fixtures × 2 threshold forms × 2 crop modes = 36 runs, every array 0 px** (`out, l, fill, index, index_floe, index_brash, index_slush, index_water, index_residue`, plus `t`, `nn_bw`, `nn_k`, sorted areas `A`, stable `ind`, `color_floe/brash`, `floe_cen/brash_cen` ≤ 1e-12, the `coverage` struct, per-piece `regionprops` centroid ≤ 1e-12 / perimeter ≤ 1e-9, the 50-bin FSD and both colour-bar blocks); **on the real 1038×394 image fed MATLAB's own `seg`/`bk` (1211 pieces = 982 light + 229 dark, 712 labels, 433 floes / 274 brash) all nine arrays 0 px**. Literalisms reproduced: `k = k − k.*bw` (dark minus overlap), `imfill` on a **double**, `r = length(p)` on the **raw** pre-fill area, `fill` from the *first* fill only, `if area0 ~= 0`, `fix` not `round`, column-major `find`. **The file needs a graphics-only patch to run in R2025a** (HG1 `get(h,'Children')`/`'Faces'`/`'FaceVertexCData'`) |
| **Eq. (7.5)** `r = r1 if size < size_th else r2` | `seaice.ch07_ice_type.adaptive_se_radius(size_ice, size_th=50, r1=1, r2=2)` + `core.morphology.strel('disk', r)` | exact | ch07 | 49 → 1, 50 → 2 (the code's `if r < se_th` agrees with the equation; the shipped `README.docx` "≤ se_th" is the outlier). **`strel('disk',1)` is the 4-connected cross (5 px), `'disk',2` is 13 px in 5×5** — so cleaning removes a rectangle's corners and an *identified* area is not the labelled area |
| **Eq. (7.6)** `Color = fix(C1(1 − e^{−area/C2}))` and its inverse `−round(C2 log(1 − c/C1))` (colour-bar ticks) | `seaice.ch07_ice_type.size_color`, `color_to_area`, `colorbar_area_ticks(colors, n=6)`; drawing in `seaice.core.plotting.size_colorbar(fig, mappable, colors, n, …)` | exact / near (`d == 0`) | ch07 | `ysh = min : fix((max−min)/n) : max`, `length(ysh)` equally spaced tick positions, labels = the inverted areas. `ysh/YT` (`n = 6`, map) and `ysh2/YT2` (`nn = 8`, histogram) identical to MATLAB on all 9 fixtures and the real image; **all 11 printed tick lists** (Figs. 7.13/7.15/7.19/7.20/7.21/7.26(a–c)/7.28(a–c)) reproduced, and the `(min colour, step)` pair for each is **unique**. ⚠ The function **saturates**: `fix(10⁴(1−e^{−A/10³}))` = 9997 for *every* `A ≥ 8112`, so 4 of the 11 lists are reproduced by any run with the same smallest piece — do **not** record that as parity. `near` only on the degenerate `d == 0` branch, where MATLAB's `min:0:max` is empty and gets no ticks while the port keeps one |
| `SIFI/sea_ice_demo.m` on **ch7's own** `sea_ice_test.jpg` (Algorithms 3→4→5) | `scripts/ch07_sea_ice_demo.py` | near | ch07 | the file is **byte-identical to ch6's** (ch06 simply never consumed `se_th 50`, `min_floe 40`, `min_brash 1`), but the **JPEG is a different encoding** (84.13 % of samples differ, max 43): Otsu 162/255 on both, `bwlabel(bw,4)` **215** vs ch6's 231. End to end: `seg` differs from MATLAB on **0.245 %**, `bk` on **0.125 %** → 1232 vs 1211 pieces, 433/290 vs 433/274 floe/brash, coverage within 0.13 pp, **identical** colour-bar ticks. The residual is ch06's `polybool`, not ch07's |
| **Algorithm 3** (p. 157) as a named entry point | `seaice.ch07_ice_type.sea_ice_edge_detection(I, **params)` → `seaice.ch06_gvf_snake.seaice_kmean_gvf` | near | ch07 | a thin wrapper (rule 9 — no second implementation); returns the three-level `SEG = 1·light + 0.5·dark` and `ICE`. Re-measured on ch7's JPEG (row above) because the input differs from ch06's |
| **Algorithm 5** (p. 160) written out from the pseudocode | `seaice.ch07_ice_type.ice_types_classification(identification, ice, T_floe=40, *, min_brash=1, book_threshold=True)` | reimplemented (book `≥`) + exact (script `>`) | ch07 | the four layers tile the image exactly once; `PIXEL = IDENTIFICATION ∪ ICE` measurably grows the ice set. **The book prints `≥`, the code writes `>`**: with `min_brash = 1` the code drops every 1-px piece from FLOE *and* BRASH → **433/290 (code)** vs **436/289 (book)** on the real image (3 pieces of exactly 40 px, 2 of 1 px). `book_threshold=False` reproduces the M-file 0 px; both forms have MATLAB references |
| **Algorithm 6** (p. 168) + §7.3.1.1 overlapping sub-images and stitching | `seaice.ch07_ice_type.tile_grid(shape, tile, overlap)`, `local_segmentation(I, tile=256, overlap=64, *, merge='crop', **params)`; `scripts/ch07_local_processing.py` | reimplemented | ch07 | **no `.m` exists** and the book fixes neither the tile size, the overlap nor the stitching rule (p. 163–164 is prose) — all three are parameters. Provable properties only: over **700 configurations** (5 heights × 4 widths × 5 tile sizes × 7 overlaps, incl. `overlap > tile/2`) the tiling covers every pixel and the overlap-removed cores partition it **exactly once** |
| §7.3.1.2 orthorectification of a **categorical** image (nearest neighbour) | `seaice.ch07_ice_type.resample_categorical(L, u, v, fill=0.0)` → `core.interp.interp2(..., 'nearest')`; **the rectifier itself is ch10 (App. A.1.1)** | exact (resampling) / deferred (rectifier) | ch07 | the pixel values are *categories* ({0, 0.5, 1}), so an averaging interpolant would invent a class; nearest-neighbour provably preserves the label set (ties round half away from zero). The book also fixes *where*: **after Algorithm 3, before Algorithm 4** (p. 166). Camera model = shooting angle 20°, FOV 46° → **ch10; do not write a second rectifier** |
| §7.3.2 sensitivity sweeps (Figs. 7.24/7.27) | `scripts/ch07_sensitivity.py --param snake\|gvf` (`BOOK_PANELS`, `--full-sweep`, `--downscale`) | unverified | ch07 | `unverified` **by construction** — the book prints only axis ranges (snake 1→122 at GVF 500; GVF 1→1500 step 20 at snake 100) and the panel tick lists, no counts. The tick agreement at snake = 1 is a **saturation coincidence** (Eq. 7.6 row), not parity. A full sweep is 25 + 75 pipeline runs |
| `core.synth.FIG_7_2_*`, `FIG_7_3_*`, `FIG_7_4_STEPS`, `FIG_7_5_*`, `FIG_7_6_*`, `FIG_7_7_STEPS` / `FIG_7_7_STEPS_BOOK` / `FIG_7_7_X8_TYPO`, `FIG_7_8_*` | `seaice.core.synth` | exact (printed truths) | ch07 | every printed matrix and block of Figs. 7.2–7.8, transcribed from `chapters/ch07.txt` (independently re-extracted at review): **48 blocks, 47 × 0 px vs MATLAB**. The 48th is a **book erratum** — Fig. 7.7(e) `X8` prints 53 px where the recursion (and MATLAB) give **55**, the two cells at 1-based (8,9)/(9,9) being forced by `X7`. Keep both variants and let MATLAB adjudicate |
